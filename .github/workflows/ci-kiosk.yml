name: Kiosk App CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'kiosk_app/**'
      - '.github/workflows/ci-kiosk.yml'

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./kiosk_app
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/srs-kiosk-app:${{ github.sha }}

      - name: Update K8s Manifest with new Image Version
        run: |
          sed -i "s|image: srs-kiosk-app:.*|image: srs-kiosk-app:${{ github.sha }}|g" k8s/base/kiosk.yaml

      - name: Commit and Push K8s Manifest
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/base/kiosk.yaml
          git commit -m "Auto-update Kiosk image version to ${{ github.sha }}"
          git push