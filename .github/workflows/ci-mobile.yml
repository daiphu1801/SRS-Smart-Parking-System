name: Mobile App CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'flutter_mobile_app/**'
      - '.github/workflows/ci-mobile.yml'

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./flutter_mobile_app
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/srs-mobile-app:${{ github.sha }}

      - name: Update K8s Manifest with new Image Version
        run: |
          sed -i "s|image: srs-mobile-app:.*|image: srs-mobile-app:${{ github.sha }}|g" k8s/base/mobile.yaml

      - name: Commit and Push K8s Manifest
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/base/mobile.yaml
          git commit -m "Auto-update Mobile image version to ${{ github.sha }}"
          git push
