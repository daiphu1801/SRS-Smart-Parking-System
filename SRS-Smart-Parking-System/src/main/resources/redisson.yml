singleServerConfig:
  address: "rediss://touched-insect-127463.upstash.io:6379"
  password: "${REDIS_PASSWORD}"

  connectionPoolSize: 10
  connectionMinimumIdleSize: 2

    # Ping 30 giây/lần để Upstash không cho Database ngủ đông
  pingConnectionInterval: 30000